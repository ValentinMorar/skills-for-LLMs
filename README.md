These skills are intended for testing. Do not use in production until you are sure they produce the desired results. 

NAAS - Numbers As A String
A string-only arithmetic specification. The goal is to make a language model
compute with numbers by manipulating digit strings through explicit reasoning
steps, without invoking any binary/numeric computation module (e.g. Python, a calculator, or built-in big-integer math).
